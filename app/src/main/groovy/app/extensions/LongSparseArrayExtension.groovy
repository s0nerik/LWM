package app.extensions

import groovy.transform.CompileStatic

@CompileStatic
class LongSparseArrayExtension {

//    static <T> List<T> filter(final LongSparseArray<T> array, @ClosureParams(value = FromString, options = ['T']) Closure<Boolean> predicate) {
//        def filtered = new ArrayList<T>()
//        for(int i = 0; i < array.size(); i++) {
//            def item = array.valueAt(i)
//            if (predicate(item)) filtered << item
//        }
//
//        return filtered
//    }
//
//    static <T> T first(final LongSparseArray<T> array, @ClosureParams(FirstParam.FirstGenericType) Closure<Boolean> predicate) {
//        for(int i = 0; i < array.size(); i++) {
//            def item = array.valueAt(i)
//            if (predicate(item)) return item
//        }
//        return null
//    }
//
//    static <T> List<T> asList(final LongSparseArray<T> array) {
//        def list = new ArrayList<T>()
//        for(int i = 0; i < array.size(); i++) {
//            list << array.valueAt(i)
//        }
//
//        return list
//    }

}